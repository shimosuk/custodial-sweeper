<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id, '社員番号' %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :name, '名前' %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :roll, '清掃可否' %><br />
    <%= f.check_box :roll, {:checked => true} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
